# compsim 사용자 매뉴얼

**에어컨 압축기 가동 타당성 판별 시뮬레이터 — 실무 가이드**

버전 0.1.0 · 2026-08-03

---

## 이 문서에 대하여

이 매뉴얼은 냉동 사이클 조건과 PMSM 파라미터를 입력해 **"이 압축기를 이 조건에서 실제로 돌릴 수 있는가"** 를 판별하는 방법을 설명합니다.

독자는 두 부류를 가정합니다.

- **바로 쓰고 싶은 분** → §1 ~ §5 만 읽으십시오. 설치, 파라미터 입력, UI 조작, 결과 해석이 전부 들어 있습니다.
- **왜 그렇게 계산되는지 알고 싶은 분** → §6 ~ §8 에 dq 변환, MTPA, 약계자, 등엔트로피 압축의 유도 과정을 단계별로 담았습니다. 사전 지식 없이 따라올 수 있게 썼습니다.

> **문서 범위에 대한 정직한 고지**
> 이 매뉴얼은 `research.md`(개념 정의)와 `plan.md`(구현 계획·변경 이력)를 근거로 작성되었으며, **현재 코드에 실제로 존재하는 기능만** 다룹니다. 아직 구현되지 않은 항목은 §9.3 에 명시했습니다.

---

## 목차

1. [설치](#1-설치)
2. [실행](#2-실행)
3. [하드웨어 파라미터 설정](#3-하드웨어-파라미터-설정)
4. [단위 변환 가이드](#4-단위-변환-가이드-가장-흔한-사고-지점)
5. [UI 조작법](#5-ui-조작법)
6. [공학적 배경 I. 열역학](#6-공학적-배경-i-열역학)
7. [공학적 배경 II. PMSM 전자기](#7-공학적-배경-ii-pmsm-전자기)
8. [공학적 배경 III. 두 도메인의 결합](#8-공학적-배경-iii-두-도메인의-결합)
9. [문제 해결 및 한계](#9-문제-해결-및-한계)

---

# 1. 설치

## 1.1 요구 사항

| 항목 | 버전 | 비고 |
|---|---|---|
| Python | 3.10 이상 | dataclass·타입 문법 의존 |
| numpy | 1.24 이상 | 유일한 필수 런타임 의존성 |
| **CoolProp** | 6.4 이상 | **설계 판단에 필수** (§1.3) |
| PyQt6 + pyqtgraph | 6.5 / 0.13 이상 | GUI 사용 시 |
| pytest | 7.4 이상 | 검증 실행 시 |

## 1.2 설치 절차

```bash
cd 2_Comp_Simulator
python -m venv .venv
source .venv/bin/activate          # Windows: .venv\Scripts\activate
pip install -e ".[all]"
```

`[all]` 은 CoolProp·PyQt6·pyqtgraph·pytest 를 모두 포함합니다. 부분 설치도 가능합니다.

```bash
pip install -e ".[accurate]"   # CoolProp 만 (CLI 전용 운용)
pip install -e ".[ui]"         # GUI 만
pip install -e ".[dev]"        # 테스트 도구만
```

## 1.3 CoolProp 은 선택이 아닙니다

압축기 흡입·토출 상태는 임계점 근방의 고밀도 증기 영역입니다. R32 를 응축 45 °C 로 돌리면 환산 압력 $P_r = P/P_c \approx 0.48$, 환산 온도 $T_r \approx 0.91$ 에 해당하고, 이 영역의 압축인자는 $Z \approx 0.7 \sim 0.9$ 입니다. **이상기체로 계산하면 밀도에서 10~30 % 가 틀어지고, 그 오차가 질량 유량 → 부하 토크로 그대로 전파됩니다.**

CoolProp 이 없으면 프로그램은 내장 근사 백엔드(`ReferenceGasBackend`)로 자동 폴백합니다. 이때 결과에는 **항상** 다음 경고가 붙습니다.

```
* REFERENCE_BACKEND_IN_USE
    CoolProp 미사용 — 근사 물성(오차 약 ±10%)으로 계산되었습니다.
    설계 판단에는 CoolProp 설치가 필요합니다.
```

이 경고가 보이면 **숫자를 설계 근거로 쓰지 마십시오.** 파이프라인이 도는지 확인하는 용도입니다.

```bash
pip install CoolProp
# 또는 (컴파일 문제가 있을 때)
conda install -c conda-forge coolprop
```

설치 후 다음 명령을 **한 번** 실행하면, CoolProp 기준 회귀 테스트가 활성화됩니다.

```bash
python tools/pin_golden.py --backend coolprop
```

## 1.4 설치 확인

```bash
pytest -q
```

기대 출력:

```
214 passed, 5 skipped
```

`5 skipped` 는 CoolProp 미설치를 뜻합니다. 설치 후 재실행하면 **219 passed** 가 되어야 합니다.

> pytest 를 설치할 수 없는 환경이라면 `python run_tests.py` 로 동일한 테스트를 실행할 수 있습니다. 테스트는 pytest fixture 를 쓰지 않도록 작성되어 두 러너 모두에서 동작합니다.

---

# 2. 실행

## 2.1 GUI

```bash
python -m compsim.ui
```

## 2.2 CLI

CLI 는 GUI 와 **완전히 동일한 코어**를 호출합니다. 자동화·배치 검토·회귀 확인에 쓰십시오.

```bash
# 내장 데모 케이스 (R32, 증발 7 °C / 응축 45 °C, 3600 rpm)
python -m compsim.cli --demo

# 냉매·회전수 변경 (R32 | R410A | R290)
python -m compsim.cli --demo --refrigerant R410A --rpm 5400
python -m compsim.cli --demo --refrigerant R290 --rpm 5400

# 속도 스윕 (lo:hi:step, 단위 rpm) + 최대 가동 회전수 탐색
python -m compsim.cli --demo --sweep 2000:16000:1000 --max-speed

# 설정 파일 사용
python -m compsim.cli --config examples/case_r32_standard.json

# 모터 파라미터 환산 결과 확인 (Ke ↔ λpm, 선간 ↔ 상 대조)
python -m compsim.cli --demo --show-motor

# 기계 판독용 JSON 출력
python -m compsim.cli --config examples/case_r32_standard.json --json
```

### 종료 코드

| 코드 | 의미 | 활용 |
|---|---|---|
| `0` | 동작 가능 (경고 포함) | CI 통과 |
| `2` | 동작 불가 | 스크립트에서 실패 처리 |

```bash
python -m compsim.cli --config case.json || echo "설계 재검토 필요"
```

## 2.3 설정 파일 형식

`examples/case_r32_standard.json` 을 복사해 수정하십시오. **입력은 실용 단위**(kPa, °C, rpm, cc/rev, mH)이며, SI 변환은 프로그램이 처리합니다.

```json
{
  "prefer_backend": "auto",
  "cycle": {
    "refrigerant": "R32",
    "T_evap_c": 7.0,
    "T_cond_c": 45.0,
    "superheat_k": 5.0,
    "subcool_k": 5.0,
    "dP_suction_kpa": 0.0,
    "dP_discharge_kpa": 0.0
  },
  "compressor": {
    "v_disp_cc": 20.0,
    "eff_preset": "SCROLL_DEFAULT",
    "drive_mode": "SPEED_DRIVEN",
    "rpm": 3600.0
  },
  "motor": {
    "Ld_mH": 3.0, "Lq_mH": 6.0,
    "ke_vrms_krpm": 30.78, "ke_reference": "LINE_TO_LINE",
    "pole_pairs": 3, "Rs_ohm": 0.5,
    "i_max_A": 20.0, "V_dc": 310.0,
    "k_margin": 0.95, "modulation": "SVPWM"
  }
}
```

### 키 설명

| 키 | 대안 | 설명 |
|---|---|---|
| `refrigerant` | `R32` / `R410A` / `R290` | §6.5 의 특성 비교 참조 |
| `T_evap_c` / `T_cond_c` | `P_evap_kpa` / `P_cond_kpa` | 온도로 주면 포화압력을 자동 조회. 압력을 직접 알면 압력 키를 쓰십시오 |
| `eff_preset` | `SCROLL_DEFAULT` / `ROTARY_DEFAULT` / `CONSTANT` | `CONSTANT` 선택 시 `eta_isen`·`eta_vol`·`eta_mech` 키를 함께 주어야 합니다 |
| `drive_mode` | `SPEED_DRIVEN` / `FLOW_DRIVEN` | 전자는 `rpm`, 후자는 `m_dot_kg_h` 필요 (§5.2) |
| `ke_vrms_krpm` | `lambda_pm_Wb` | 역기전력 상수(권장). 구버전 설정의 `lambda_pm_Wb` 도 계속 인식됩니다 |
| `ke_reference` | `LINE_TO_LINE` / `PHASE` | 생략 시 `LINE_TO_LINE`. §3.3 참조 |
| `prefer_backend` | `auto` / `coolprop` / `reference` | `coolprop` 은 미설치 시 오류로 중단 — CI 에서 유용 |

---

# 3. 하드웨어 파라미터 설정

여기가 실무에서 가장 자주 틀리는 구간입니다. 각 파라미터를 **어디서 얻고, 어떻게 확인하는지** 순서대로 설명합니다.

## 3.1 DC 링크 전압 $V_{dc}$

인버터 직류단(평활 커패시터 양단) 전압입니다.

### 값 정하기

| 입력 방식 | $V_{dc}$ | 근거 |
|---|---|---|
| 단상 220 V AC, 다이오드 정류 | **약 310 V** | $220 \times \sqrt2 = 311$ V (무부하 피크) |
| 단상 220 V AC, 부하 상태 | 280 ~ 300 V | 리플·전압강하로 하강. 보수적 검토 시 이 값 |
| 삼상 380 V AC, 다이오드 정류 | 약 537 V | $380 \times \sqrt2$ |
| PFC 승압단 있음 | 380 ~ 400 V | PFC 출력 지령값을 그대로 |

> **실무 조언**: 최악 조건 검토가 목적이라면 **계통 저전압(예: 정격의 −10 %) 상태의 $V_{dc}$** 를 넣으십시오. 전압 한계는 고속에서 지배적이므로, $V_{dc}$ 를 낙관적으로 잡으면 현장에서 탈조가 납니다.

### $V_{dc}$ 에서 상전압 한계로

프로그램은 $V_{dc}$ 를 바로 쓰지 않고 **상전압 피크 한계** $v_{max}$ 로 환산합니다.

$$v_{max} = k_{margin} \cdot \frac{V_{dc}}{\sqrt3} \quad (\text{SVPWM})$$
$$v_{max} = k_{margin} \cdot \frac{V_{dc}}{2} \quad (\text{SPWM})$$

**왜 $\sqrt3$ 인가**: SVPWM 은 3상 인버터의 8개 스위칭 벡터를 조합해 선형 영역에서 육각형에 내접하는 원까지 합성할 수 있습니다. 이 원의 반지름이 $V_{dc}/\sqrt3$ 입니다. SPWM 은 각 상을 독립적으로 정현파 변조하므로 $V_{dc}/2$ 에 그칩니다. **SVPWM 이 약 15.5 % 더 높은 전압을 씁니다** ($2/\sqrt3 = 1.1547$).

기본값 `V_dc = 310`, `k_margin = 0.95`, SVPWM 이면:

$$v_{max} = 0.95 \times \frac{310}{1.7321} = 170.0\ \mathrm{V\ (peak)}$$

### $k_{margin}$ 정하기

데드타임, IGBT/MOSFET 포화 전압, 전류 리플 여유를 포괄하는 계수입니다.

| 상황 | 권장 $k_{margin}$ |
|---|---|
| 일반 설계 검토 | **0.95** (기본값) |
| 데드타임이 긴 저가 드라이브 | 0.90 |
| 과변조 영역까지 쓰는 고성능 제어 | 0.98 (단, 고조파 증가 감수) |

> **과변조(overmodulation)와 6-step 은 현재 지원하지 않습니다.** 이론상 $2V_{dc}/\pi$ 까지 올릴 수 있지만 고조파가 급증하고 토크 리플·소음이 커지므로, 본 도구는 **선형 변조 영역만** 판정합니다. 과변조로 운용할 계획이라면 `k_margin` 을 1.0 이상으로 올리는 대신 그 결과를 낙관적 추정으로 해석하십시오.

## 3.2 인덕턴스 $L_d$, $L_q$

d축·q축 동기 인덕턴스입니다. **밀리헨리(mH) 단위로 입력**합니다.

> **UI 라벨**: 입력 필드는 `d축 인덕턴스 Ld (상)` / `q축 인덕턴스 Lq (상)` 으로 표시되어, 상값을 요구한다는 점이 화면에서 바로 드러납니다. 마우스를 올리면 선간 측정 시 2로 나누라는 안내가 나옵니다.

### 획득 방법

| 방법 | 절차 | 정확도 |
|---|---|---|
| 모터 데이터시트 | 그대로 사용 | 규약 확인 필요 (아래) |
| LCR 미터 실측 | 로터를 d축/q축에 정렬 후 상간 측정 | 중간 (포화 미반영) |
| 유한요소(FEA) | 전류 수준별 맵 추출 | 높음 |
| 정지형 파라미터 동정 | 인버터가 전압 스텝 인가 후 전류 응답 관측 | 높음 |

### ⚠️ 상 인덕턴스인가, 선간 인덕턴스인가

**LCR 미터로 두 단자 사이를 재면 그것은 선간(line-to-line) 값이며, 상(phase) 값의 2배입니다.**

$$L_{phase} = \frac{L_{line-to-line}}{2}$$

Y 결선에서 두 단자 사이에는 두 상 권선이 직렬로 놓이기 때문입니다. 프로그램은 **상 인덕턴스**를 요구합니다.

```
LCR 미터 표시:  6.0 mH  (U-V 단자 간)
프로그램 입력:  3.0 mH  ← 2로 나눌 것
```

> **자가 점검**: $L_q/L_d$(돌극비)가 1.5 ~ 3.0 범위를 벗어나면 규약 혼동을 의심하십시오. 가정용 압축기 IPMSM 은 보통 1.5 ~ 2.5 입니다. 기본값 3.0/6.0 mH 는 돌극비 2.0 입니다.

### $L_d < L_q$ 인지 확인

IPMSM(매입형)은 **반드시 $L_q > L_d$** 입니다. 자석의 투자율이 공기와 비슷해서 자석을 지나는 d축 경로의 자기저항이 크기 때문입니다.

- $L_q > L_d$ → IPMSM. 릴럭턴스 토크를 쓸 수 있고, MTPA 가 $i_d < 0$ 을 선택합니다.
- $L_q = L_d$ → SPMSM(표면부착형). 릴럭턴스 토크 없음. 프로그램이 자동으로 SPMSM 분기로 처리합니다 ($|L_q - L_d| < 10^{-9}$ H 판정).
- $L_q < L_d$ → **입력 오류일 가능성이 큽니다.** GUI 가 경고를 표시합니다.

### 포화 영향

$L_d$, $L_q$ 는 실제로는 전류에 따라 변합니다(자기 포화). 본 도구는 **상수로 취급**하므로, 파라미터는 **실제 운전 전류 근방에서 측정한 값**을 넣으십시오. 무부하 측정값을 넣으면 고전류 영역에서 토크를 과대평가합니다.

## 3.3 역기전력 상수 $K_e$ [Vrms/krpm]

> **v0.2 변경**: 이전에는 쇄교자속 $\lambda_{pm}$ [Wb] 를 직접 입력했으나, 실무에서 계측·데이터시트로 얻는 값은 역기전력 상수이므로 **$K_e$ 입력으로 전환**했습니다. $\lambda_{pm}$ 은 프로그램이 환산해 화면에 표시합니다.

### 정의

$$K_e = \frac{\text{무부하 역기전력 실효값}}{\text{회전수 [krpm]}} \quad [\mathrm{Vrms/krpm}]$$

즉 **1000 rpm 에서 측정한 무부하 역기전력 실효값**이 곧 $K_e$ 입니다.

### 선간 기준인가, 상 기준인가 — √3 배 차이

이것이 이 파라미터의 유일한 함정입니다. 프로그램은 **기준을 명시적으로 선택**하게 합니다.

| 기준 | 의미 | 비고 |
|---|---|---|
| **선간 (line-to-line)** | 두 단자 사이 전압 실효값 | **PMSM 데이터시트의 지배적 관행. 기본값** |
| 상 (phase) | 중성점 기준 한 상 전압 실효값 | 선택 시 프로그램이 경고를 표시 |

같은 숫자를 상 기준으로 잘못 읽으면 **쇄교자속이 √3 = 1.732 배 과대평가되어 토크가 73 % 틀어집니다.**

### 측정 절차

1. 모터를 커플링에서 분리하고 외부 구동기로 일정 속도(예: 1000 rpm)로 회전시킵니다
2. 단자를 개방한 상태에서 **선간 전압 실효값**을 측정합니다 (오실로스코프 또는 True-RMS 미터)
3. $K_e = E_{LL,rms} / N_{krpm}$

**예**: 1000 rpm 에서 선간 실효값 48.0 V 측정 → $K_e = 48.0$ Vrms/krpm (선간)

### 내부 환산식

프로그램은 입력된 $K_e$ 를 코어 물리량인 $\lambda_{pm}$ 으로 **단 한 번** 환산합니다.

$$\lambda_{pm} = \frac{60\sqrt2}{2\pi \cdot 1000 \cdot \sqrt3 \cdot p}\, K_e \quad(\text{선간 기준})$$

$$\lambda_{pm} = \frac{60\sqrt2}{2\pi \cdot 1000 \cdot p}\, K_e \quad(\text{상 기준})$$

**유도 과정** (선간 기준):

1. 상 역기전력 피크는 정의상 $E_{ph,peak} = \omega_e \lambda_{pm}$
2. 상 실효값 $E_{ph,rms} = E_{ph,peak}/\sqrt2$
3. Y 결선 선간 실효값 $E_{LL,rms} = \sqrt3\,E_{ph,rms}$
4. 1 krpm 에서 $\omega_e = p \cdot 2\pi \cdot 1000/60$ 이고 $E_{LL,rms} = K_e$
5. 대입해 $\lambda_{pm}$ 에 대해 정리

**검산**: $K_e = 48.0$ Vrms/krpm(선간), $p = 3$ 이면

$$\lambda_{pm} = \frac{60 \times 1.41421}{6283.19 \times 1.73205 \times 3} \times 48.0 = 0.1248\ \mathrm{Wb}$$

### 기본값

| 표현 | 값 |
|---|---|
| $K_e$ (선간) | **30.78** Vrms/krpm |
| $K_e$ (상) | 17.77 Vrms/krpm |
| $\lambda_{pm}$ (환산) | 0.08000 Wb |

### 다른 표기에서 변환하기

| 데이터시트 표기 | $K_e$ [Vrms/krpm, 선간] 로 변환 |
|---|---|
| $\lambda_{pm}$ [Wb] 또는 $K_e$ [V·s/rad, 상 peak] | $\times\ \dfrac{2\pi \cdot 1000\sqrt3\,p}{60\sqrt2}$ |
| $K_e$ [Vpk/krpm, 선간] | $\div\sqrt2$ |
| $K_e$ [Vrms/krpm, 상] | $\times\sqrt3$ 하거나, **기준 콤보를 '상'으로 변경** |
| 토크 상수 $K_t$ [N·m/A, peak] (SPMSM) | $\lambda_{pm} = \dfrac{2K_t}{3p}$ 로 구한 뒤 위 첫 행 적용 |

### 검산 방법

GUI 모터 그룹의 **환산 결과** 칸, 또는 CLI `--show-motor` 가 다음을 표시합니다.

```
 역기전력 상수 Ke   30.780 Vrms/krpm (선간) = 17.771 (상)
 쇄교자속 λpm       0.08000 Wb  (환산값)
 1000rpm 선간 역기전력  30.78 Vrms   ← 실측 대조용
```

마지막 줄을 **실측값과 직접 비교**하십시오. 일치하지 않으면 기준(선간/상) 또는 극쌍수가 틀린 것입니다.


## 3.4 극쌍수 $p$

**극수가 아니라 극쌍수입니다.**

| 모터 극수 | 입력할 $p$ |
|---|---|
| 4극 | 2 |
| **6극** | **3** (기본값) |
| 8극 | 4 |

이 값을 2배로 틀리면 토크가 2배 틀어집니다. 확인법: 정격 회전수와 정격 전기 주파수를 알면

$$p = \frac{60 \cdot f_e}{N_{rpm}}$$

예: 3600 rpm 에서 인버터 출력 주파수가 180 Hz 라면 $p = 60 \times 180 / 3600 = 3$.

## 3.5 상저항 $R_s$

**상당(per-phase) 저항**입니다. UI 라벨은 `상저항 Rs (상)` 입니다. LCR/밀리옴 미터로 두 단자 사이를 재면 선간 값이므로 역시 2로 나눕니다.

$$R_s = \frac{R_{line-to-line}}{2}$$

온도 보정도 고려하십시오. 구리 권선은 $\approx 0.393\ \%/\mathrm{K}$ 로 저항이 증가합니다.

$$R_s(T) = R_s(20°\mathrm{C}) \cdot \left[1 + 0.00393 (T - 20)\right]$$

권선이 100 °C 로 상승하면 저항은 약 31 % 증가합니다. **본 도구는 $R_s$ 를 상수로 취급하므로, 운전 온도 기준값을 넣는 것이 보수적입니다.**

> $R_s$ 는 저속·고토크 영역에서 전압 판정에 유의미하게 기여합니다. 프로그램은 판정에 $R_s$ 를 **포함한 정확식**을 쓰고, 화면의 타원은 $R_s$ 를 무시한 근사입니다 (§5.4).

## 3.6 최대 허용 전류 $i_{max}$

**인버터 소자 정격**과 **모터 권선 열 정격** 중 작은 값을 넣습니다. 단위는 A, **peak 기준**입니다.

데이터시트가 RMS 로 주어졌다면 GUI 의 "전류 정격이 RMS 기준" 체크박스를 켜거나, 직접 $\sqrt2$ 를 곱해 입력하십시오 (§4.2).

| 정격 종류 | 사용 상황 |
|---|---|
| 연속 정격 | 정상 운전 판정 (기본 가정) |
| 순시/피크 정격 | 기동 토크·과도 검토 — 단, 본 도구는 정상상태 전용이므로 해석에 주의 |

## 3.7 파라미터 요약표

| 파라미터 | 기본값 | 단위 | 규약 |
|---|---|---|---|
| $L_d$ | 3.0 | mH | **상(phase)** 인덕턴스 |
| $L_q$ | 6.0 | mH | **상(phase)** 인덕턴스, $> L_d$ |
| $K_e$ | 30.78 | Vrms/krpm | **선간(line-to-line)** 실효값 |
| $p$ | 3 | – | **극쌍수** |
| $R_s$ | 0.5 | Ω | **상당(per-phase)** 저항 |
| $i_{max}$ | 20.0 | A | **peak** |
| $V_{dc}$ | 310.0 | V | 직류단 |
| $k_{margin}$ | 0.95 | – | (0, 1] |

$K_e = 30.78$ Vrms/krpm(선간), $p=3$ 은 $\lambda_{pm} = 0.08000$ Wb 에 대응합니다.

기본값 조합의 **특성 전류**는 $i_{ch} = \lambda_{pm}/L_d = 0.08/0.003 = 26.7$ A 로, $i_{max} = 20$ A 보다 큽니다. 이는 **유한한 최대 속도를 갖는 모터**라는 뜻입니다 (§7.7).

---

# 4. 단위 변환 가이드 (가장 흔한 사고 지점)

`research.md` §5 는 이 항목을 **"가장 흔하고 치명적인 리스크(R1)"** 로 분류합니다. 실제로 토크가 $\sqrt2$ 배 또는 2배 틀어지는 사고가 여기서 납니다.

> **현재 구현 상태 (v0.2)**: 프로그램이 자동 처리하는 변환은 §4.5 표에 정리했습니다.
> - **역기전력의 선간↔상 변환은 자동화되었습니다** — §3.3 의 기준 콤보로 선택하십시오.
> - **Hz↔rpm 변환은 여전히 수동**입니다. §4.4 절차로 환산해 입력하십시오.
> - 인덕턴스·저항의 선간↔상(÷2)은 **측정 단계의 문제**이므로 자동화 대상이 아닙니다. §3.2·§3.5 참조.

## 4.1 프로그램의 내부 규약

내부 연산은 **amplitude-invariant (peak) dq 변환**을 사용합니다. 코드 상수로 고정되어 있습니다.

```python
TORQUE_DQ_COEFF = 1.5
DQ_CONVENTION   = "amplitude-invariant (peak)"
```

이 규약에서:

- 토크식 계수가 $\tfrac32$ 입니다: $T = \tfrac32 p [\lambda_{pm} i_q + (L_d - L_q) i_d i_q]$
- $i_d, i_q, v_d, v_q, i_{max}, v_{max}$ 가 **전부 peak(진폭)** 값입니다
- 정지 좌표계에서 상전류 진폭이 $I$ 이면 dq 평면에서도 $\sqrt{i_d^2+i_q^2} = I$ 입니다

> **다른 규약과의 차이**: power-invariant 변환($\sqrt{2/3}$ 스케일링)을 쓰면 토크식 계수가 1.0 이 되고 전류 크기가 $\sqrt{3/2}$ 배 달라집니다. 사내 문서가 power-invariant 기준이라면 값을 그대로 옮기지 마십시오.

## 4.2 전류: RMS ↔ peak

정현파 3상 전류에서

$$I_{peak} = \sqrt2 \cdot I_{rms}$$

| 데이터시트 표기 | 입력 방법 |
|---|---|
| "정격 전류 14.1 A (rms)" | GUI 체크박스 켜고 14.1 입력 → 내부 19.9 A |
| "피크 전류 20 A" | 체크박스 끄고 20 입력 |

GUI 의 **"전류 정격이 RMS 기준"** 체크박스가 이 변환을 수행합니다. CLI/JSON 에는 이 옵션이 없으므로 **직접 $\sqrt2$ 를 곱해서** `i_max_A` 에 넣으십시오.

```
데이터시트 14.1 A(rms) → JSON 에는 "i_max_A": 19.94
```

## 4.3 전압: 선간 ↔ 상, RMS ↔ peak

Y 결선 기준 변환 사슬입니다.

$$V_{LL,rms} \xrightarrow{\ \div\sqrt3\ } V_{ph,rms} \xrightarrow{\ \times\sqrt2\ } V_{ph,peak}$$

합치면:

$$V_{ph,peak} = \frac{\sqrt2}{\sqrt3} V_{LL,rms} = 0.8165 \cdot V_{LL,rms}$$

**이 변환은 이제 프로그램이 처리합니다** — §3.3 의 `Ke 기준` 콤보에서 선간/상을 고르면 내부에서 $\sqrt3$ 이 적용됩니다. 아래 관계는 실측값을 검산할 때 쓰십시오.

$v_{max}$ 자체는 프로그램이 $V_{dc}$ 에서 계산하므로 직접 입력하지 않습니다.

### 검산 예

$V_{dc} = 310$ V, SVPWM, $k_{margin}=0.95$ → $v_{max} = 170.0$ V(peak).
이를 역으로 환산하면:

$$V_{ph,rms} = 170.0/\sqrt2 = 120.2\ \mathrm{V}, \qquad V_{LL,rms} = 120.2 \times \sqrt3 = 208.2\ \mathrm{V}$$

220 V 계통에서 208 V 선간 실효값이 나오는 것은 타당합니다(마진 5 % 포함).

## 4.4 주파수: Hz ↔ rpm ↔ rad/s

세 가지 속도 표현을 구분하십시오.

| 기호 | 이름 | 관계 |
|---|---|---|
| $N$ [rpm] | 기계 회전수 | 축이 도는 속도 |
| $\omega_m$ [rad/s] | 기계 각속도 | $\omega_m = 2\pi N / 60$ |
| $\omega_e$ [rad/s] | **전기** 각속도 | $\omega_e = p \cdot \omega_m$ |
| $f_e$ [Hz] | **전기** 주파수 | $f_e = \omega_e / 2\pi = p N / 60$ |

**프로그램에는 rpm 을 입력합니다.** 인버터 설정이 Hz 기준이라면 다음으로 환산하십시오.

$$N_{rpm} = \frac{60 \cdot f_e}{p}$$

### 변환표 ($p = 3$, 6극 기준)

| $f_e$ [Hz] | $N$ [rpm] | $\omega_e$ [rad/s] |
|---|---|---|
| 30 | 600 | 188.5 |
| 60 | 1200 | 377.0 |
| **180** | **3600** | **1131.0** |
| 360 | 7200 | 2261.9 |
| 600 | 12000 | 3769.9 |

> **흔한 실수**: 인버터가 "180 Hz" 를 표시할 때 이를 회전 주파수로 착각해 10800 rpm 을 입력하는 경우가 있습니다. 인버터 출력 주파수는 **전기** 주파수입니다.

## 4.5 프로그램이 자동 처리하는 변환

GUI/CLI 에 **실용 단위로 입력하면** 아래 변환은 자동입니다. `units.py` 가 유일한 변환 창구이며, 코어 연산은 전량 SI 로 수행됩니다.

| 입력 단위 | 내부 SI | 함수 |
|---|---|---|
| °C | K | `celsius_to_kelvin` |
| kPa, bar | Pa | `kpa_to_pa`, `bar_to_pa` |
| rpm | rev/s, rad/s | `rpm_to_rev_s`, `rpm_to_rad_s` |
| mH | H | `mh_to_h` |
| cc/rev | m³/rev | `cc_per_rev_to_m3_per_rev` |
| kg/h | kg/s | `kg_h_to_kg_s` |
| RMS | peak | `peak_from_rms` (GUI 체크박스) |
| $V_{dc}$ | $v_{max}$ | `v_max_from_vdc` |
| $\omega_m$ | $\omega_e$ | `mech_to_elec_speed` |
| $K_e$ [Vrms/krpm] | $\lambda_{pm}$ [Wb] | `lambda_pm_from_ke` (기준 콤보) |
| $\lambda_{pm}$ | $K_e$ | `ke_from_lambda_pm` (역변환, 표시용) |
| $\lambda_{pm}$, rpm | 선간 역기전력 [Vrms] | `back_emf_line_to_line_rms` (실측 대조용) |
| 선간 | 상 | `phase_from_line_to_line` |
| 선간 $L$, $R$ | 상 $L$, $R$ | `phase_inductance_from_line_to_line`, `phase_resistance_from_line_to_line` |

## 4.6 입력 전 최종 점검표

```
□ L_d, L_q 는 상 인덕턴스인가?  (LCR 선간 측정값이면 ÷2)
□ L_q > L_d 인가?              (IPMSM 이면 반드시)
□ 돌극비 L_q/L_d 가 1.5~3.0 인가?
□ K_e 의 기준이 선간인가 상인가?  (콤보 확인 — √3 차이)
□ K_e 가 실효값(rms)인가?       (peak 표기면 ÷√2)
□ p 는 극쌍수인가?             (6극 → 3)
□ R_s 는 상당 저항인가?         (선간이면 ÷2)
□ i_max 는 peak 인가?          (RMS 면 ×√2 또는 체크박스)
□ rpm 은 기계 회전수인가?       (인버터 Hz 면 ×60/p)
□ V_dc 는 부하 상태 값인가?     (무부하 피크는 낙관적)

**최종 확인**: `--show-motor` 또는 GUI '환산 결과' 칸의 `1000rpm 선간 역기전력` 값을 실측값과 대조하십시오. 여기가 맞으면 K_e·p·기준이 모두 맞습니다.
```

---

# 5. UI 조작법

```bash
python -m compsim.ui
```

## 5.1 화면 구성

```
┌──────────────┬─────────────────────────────┬──────────────────┐
│ 입력 패널     │  [d–q 전류 평면] [속도 스윕]  │  결과 패널        │
│ (스크롤)      │                             │                  │
│              │                             │  ┌────────────┐  │
│ 냉매·모드     │      (탭 전환)               │  │  판정 배지  │  │
│ 냉매·사이클   │                             │  └────────────┘  │
│ 압축기        │                             │  위반 목록        │
│ 모터(PMSM)   │                             │  (FAIL 먼저)      │
│ 열적 한계     │                             │                  │
│              │                             │  수치 테이블      │
│ [재계산]      │                             │                  │
└──────────────┴─────────────────────────────┴──────────────────┘
                    상태 표시줄: 최대 가동 회전수 / 경고
```

세 영역은 **스플리터**로 나뉘어 있어 경계를 끌어 폭을 조절할 수 있습니다.

**값을 바꾸면 즉시 재계산됩니다.** 수동 재계산은 `재계산` 버튼 또는 **Ctrl+R**.

## 5.2 토글 ① — 구동 모드 (SPEED_DRIVEN / FLOW_DRIVEN)

`냉매 · 모드` 그룹의 **구동 모드** 콤보로 전환합니다. 선택에 따라 아래쪽 입력 필드가 자동으로 활성/비활성됩니다.

| 모드 | 입력 | 산출 | 관점 |
|---|---|---|---|
| **SPEED_DRIVEN** | 회전수 [rpm] | 질량 유량 | **인버터 제어 엔지니어** — "이 속도로 돌리면 유량이 얼마?" |
| **FLOW_DRIVEN** | 질량 유량 [kg/h] | 회전수 | **사이클 설계자** — "이 냉방 능력을 내려면 몇 rpm?" |

두 모드는 **동일한 하나의 관계식**을 서로 다른 미지수로 푸는 것입니다.

$$\dot m = \rho_{suc} \cdot V_{disp} \cdot N \cdot \eta_{vol}$$

따라서 SPEED_DRIVEN 으로 3600 rpm → 103.9 kg/h 를 얻은 뒤 FLOW_DRIVEN 에 103.9 kg/h 를 넣으면 정확히 3600 rpm 이 돌아옵니다(왕복 항등성, 테스트로 보장).

> **주의**: FLOW_DRIVEN 모드에서는 **속도 스윕 탭이 갱신되지 않습니다.** 유량이 고정된 상태에서 속도를 스윕한다는 것이 물리적으로 모순이기 때문입니다. 상태 표시줄에 안내가 나옵니다.

## 5.3 토글 ② — 전류 정격 RMS/peak 체크박스

`냉매 · 모드` 그룹 하단의 **"전류 정격이 RMS 기준"** 체크박스입니다.

- **꺼짐(기본)**: `최대 허용 전류` 입력값을 peak 로 그대로 사용
- **켜짐**: 입력값에 $\sqrt2$ 를 곱해 peak 로 변환

체크박스에 마우스를 올리면 툴팁으로 규약 설명이 나옵니다. 판정 결과가 데이터시트 예상과 $\sqrt2$ 배 어긋난다면 **가장 먼저 이 체크박스를 의심하십시오.**

## 5.4 토글 ③ — Ke 기준 (선간 / 상)

`냉매 · 모드` 그룹의 **Ke 기준** 콤보입니다.

| 선택 | 해석 | 결과 |
|---|---|---|
| **선간 (line-to-line)** — 기본 | 입력값을 선간 실효값으로 | $\lambda_{pm} = K_e \cdot 0.0077968/p$ |
| 상 (phase) | 입력값을 상 실효값으로 | $\lambda_{pm} = K_e \cdot 0.0135044/p$ (√3 배 큼) |

상 기준을 고르면 상태 표시줄에 경고가 나옵니다 — 데이터시트 대다수가 선간 기준이기 때문입니다.

**즉시 검산**: 모터 그룹 하단 **환산 결과** 칸이 다음을 실시간 표시합니다.

```
λpm = 0.08000 Wb (상 peak)
1000 rpm 무부하 선간 역기전력 = 30.78 Vrms
특성 전류 i_ch = λpm/Ld = 26.67 A
```

두 번째 줄을 실측값과 비교하면 기준·극쌍수 오류가 즉시 드러납니다.

## 5.5 토글 ④ — 기타 콤보 박스

| 콤보 | 선택지 | 영향 |
|---|---|---|
| **냉매** | R32 / R410A / R290 | 물성 전체. 토출 온도와 체적 능력이 크게 갈립니다 (§6.5) |
| **압축기 프리셋** | 스크롤 (기본) / 로터리 | 효율 상관식 계수. 로터리는 클리어런스 손실이 큼 |
| **변조 방식** | SVPWM / SPWM | $v_{max}$ 가 $V_{dc}/\sqrt3$ 또는 $V_{dc}/2$ (§3.1) |

## 5.6 d–q 전류 평면 탭

**가로축 $i_d$, 세로축 $i_q$**, 종횡비 1:1 고정(원이 원으로 보여야 하므로).

### 표시 요소

| 요소 | 색 | 스타일 | 의미 |
|---|---|---|---|
| 전류 한계 원 | 파랑 | 실선 | 원점 중심, 반지름 $i_{max}$. 이 **밖**은 소자/권선 과열 |
| 전압 한계 타원 | 초록 | 실선 | $R_s$ 무시 근사. 속도가 오르면 축소 |
| 전압 한계 (정확) | 연초록 | 파선 | **$R_s$ 포함 — 실제 판정 기준** |
| 상수 토크 곡선 | 주황 | 실선 | 요구 토크를 내는 모든 $(i_d, i_q)$ 조합 |
| MTPA 궤적 | 회색 | 파선 | 같은 전류로 최대 토크를 내는 경로 (참조선) |
| 운전점 | 초록●/빨강● | 마커 | 실제 선택된 동작점. 색이 판정 결과 |
| 특성 전류점 | 회색✕ | 마커 | $(-\lambda_{pm}/L_d,\ 0)$ — 전압 타원의 중심 |
| 동일 $i_q$ 의 MTPA 점 | 회색▲ | 마커 | 약계자 시에만 표시. MTPA 대비 얼마나 벗어났는지 |

### 읽는 법

**정상 상태(저속)**: 운전점이 MTPA 파선 위에 있고, 전류 원과 전압 타원 **둘 다 내부**에 있습니다.

**약계자 진입(중속)**: 운전점이 MTPA 파선을 벗어나 **왼쪽 아래**(더 음의 $i_d$)로 이동하고, **전압 경계에 붙어** 있습니다. 같은 토크를 내는데 전류가 더 필요해진 상태입니다.

**동작 불가(고속)**: 운전점 마커가 사라집니다. 주황 상수 토크 곡선이 전압 타원 밖에 완전히 놓여 있거나, 전류 원 밖으로 밀려나 있습니다. **두 도형이 어디서 어긋났는지 눈으로 확인**하십시오 — 그것이 원인입니다.

> **두 개의 전압 경계가 왜 다른가**: 초록 실선은 $R_s$ 를 무시한 이상적 타원으로, 교과서 그림과 같아 직관적입니다. 연초록 파선은 $R_s i$ 강하를 포함한 실제 경계로, 저항 때문에 회전·이동합니다. **판정은 파선 기준**입니다. 저속에서 두 선의 간격이 벌어지는 것이 정상입니다.

## 5.7 속도 스윕 탭

**실무에서 가장 쓸모 있는 화면**입니다. 단일 동작점의 가부보다 "어디까지 돌릴 수 있는가"가 실제 설계 질문이기 때문입니다.

- **초록 선**: 최대 가용 토크 $T_{max}^{avail}(\omega)$ — 속도가 오르면 전압 타원 축소로 감소
- **주황 선**: 부하 토크 $T_{load}$ — 거의 수평 (§8.2 참조)
- **빨강 파선**: 두 선이 만나는 지점 = **최대 가동 회전수**

스윕 범위는 현재 입력 회전수의 3배(최소 12000 rpm)까지 40점으로 자동 설정됩니다. 상태 표시줄에도 같은 값이 표시됩니다.

**해석 예**: 초록 선이 3000 rpm 부근에서 급격히 꺾이면 그 지점이 기저 속도(약계자 진입점)입니다. 그 이후 기울기가 가파를수록 $V_{dc}$ 여유가 부족하다는 뜻입니다.

## 5.8 결과 패널

### 판정 배지

| 배지 | 색 | 의미 |
|---|---|---|
| 동작 가능 (FEASIBLE) | 초록 | 모든 제한 만족 |
| 동작 가능 — 경고 있음 | 주황 | 운전은 되지만 확인 필요 |
| 동작 불가 (INFEASIBLE) | 빨강 | FAIL 항목 존재 |

조립 규칙은 단순합니다. **FAIL 이 하나라도 있으면 INFEASIBLE**, 없고 WARN 만 있으면 경고, 둘 다 없으면 FEASIBLE.

### 위반 목록

**FAIL 이 항상 위에** 옵니다(빨강 굵은 글씨). 원인을 먼저 보라는 의도입니다. 각 항목은 코드와 한국어 설명, 실제값·한계값을 함께 보여줍니다.

### 수치 테이블

부하 토크, 최대 가용 토크, 토출 온도, 토크 여유율은 **굵게** 강조됩니다.

$$\text{토크 여유율} = 1 - \frac{T_{load}}{T_{max}^{avail}}$$

- **양수**: 여유 있음. 40 % 면 정격의 60 % 만 쓰고 있다는 뜻
- **음수**: 요구 토크가 가용 토크를 초과 — 얼마나 부족한지 정량 표시
- **"해당 없음 (가용 토크 0)"**: 전류 원과 전압 타원이 완전히 분리된 상태

## 5.9 입력 오류 처리

물리적으로 성립하지 않는 입력은 **팝업으로 차단**되고 계산이 진행되지 않습니다.

| 차단되는 입력 | 메시지 |
|---|---|
| 응축 온도 ≤ 증발 온도 | "응축 온도(45.0 °C)는 증발 온도(50.0 °C)보다 높아야 합니다." — 실제 입력값이 함께 표시됩니다 |
| 행정체적 ≤ 0 | "행정체적은 0보다 커야 합니다." |
| 인덕턴스 ≤ 0 | "인덕턴스는 0보다 커야 합니다." |
| 극쌍수 < 1 | "극쌍수는 1 이상의 정수여야 합니다." |
| $i_{max}$ 또는 $V_{dc}$ ≤ 0 | "최대 전류와 직류단 전압은 0보다 커야 합니다." |
| $k_{margin} \notin (0,1]$ | "전압 마진 계수는 (0, 1] 범위여야 합니다." |
| 회전수 ≤ 0 (SPEED_DRIVEN) | "회전수는 0보다 커야 합니다." |
| 질량 유량 ≤ 0 (FLOW_DRIVEN) | "질량 유량은 0보다 커야 합니다." |

반면 **경고**는 계산을 막지 않고 상태 표시줄에 표시됩니다 — $L_q < L_d$, 과열도 ≤ 0, 열적 게이트 순서 문제 등.

---

# 6. 공학적 배경 I. 열역학

여기서부터는 프로그램이 **무엇을 왜 계산하는지** 설명합니다. 사전 지식 없이 따라올 수 있게 단계적으로 씁니다.

## 6.1 압축기가 하는 일

냉동 사이클에서 압축기는 **저압 증기를 고압으로 밀어 올리는** 역할을 합니다. 이 과정에 일이 들어가고, 그 일은 모터 축에서 나옵니다. 우리가 알고 싶은 것은 **축에 걸리는 토크**입니다.

계산은 네 개의 상태점을 거칩니다.

| 점 | 위치 | 결정 방식 |
|---|---|---|
| **1** | 압축기 흡입 | $T_1 = T_{sat}(P_1) + \Delta T_{sh}$ |
| **2s** | 등엔트로피 토출 (이상) | $P_2$, $s_{2s} = s_1$ |
| **2** | 실제 토출 | $h_2 = h_1 + (h_{2s}-h_1)/\eta_{isen}$ |
| **3** | 응축기 출구 | $T_3 = T_{sat}(P_2) - \Delta T_{sc}$ |

## 6.2 등엔트로피 압축 일

**엔트로피**는 거칠게 말해 "무질서도"이고, 손실 없는 이상적 압축은 엔트로피가 변하지 않습니다. 이것이 이론적 최소 일입니다.

$$w_{isen} = h_{2s} - h_1 = h(P_2, s_1) - h(P_1, T_1) \quad [\mathrm{J/kg}]$$

여기서 $h$ 는 비엔탈피(단위 질량당 에너지)입니다. CoolProp 이 상태방정식으로 이 값을 줍니다.

실제 압축기는 마찰·누설·열전달로 더 많은 일을 씁니다.

$$w_{act} = \frac{w_{isen}}{\eta_{isen}}$$

$\eta_{isen}$(등엔트로피 효율)이 0.70 이면 이론값의 1/0.70 = 1.43 배를 쓴다는 뜻입니다.

**여기서 중요한 물리**: 초과분은 사라지지 않고 **열로 바뀌어 냉매 온도를 올립니다.** 그래서 실제 토출 엔탈피 $h_2$ 가 $h_{2s}$ 보다 크고, 실제 토출 온도가 이론값보다 높습니다. §6.5 의 토출 온도 문제가 여기서 나옵니다.

## 6.3 축 동력과 부하 토크

$$P_{indicated} = \dot m \cdot w_{act}, \qquad P_{shaft} = \frac{P_{indicated}}{\eta_{mech}}$$

$\eta_{mech}$ 는 베어링 마찰·오일 점성 손실입니다. 그리고

$$\boxed{\ T_{load} = \frac{P_{shaft}}{\omega_m}\ }, \qquad \omega_m = 2\pi N$$

**이 한 줄이 열역학 도메인의 유일한 출력**이며, 전자기 도메인의 유일한 입력입니다.

> 모터 자체의 동손·철손은 여기 포함되지 않습니다. 축이 감당할 토크만 다루며, 전기 입력은 별개입니다. 이중 계상을 피하기 위한 의도적 분리입니다.

## 6.4 효율 상관식

프로그램은 세 효율을 압력비 $PR = P_{dis}/P_{suc}$ 의 함수로 모델링합니다.

$$\eta_{isen}(PR) = a_0 + a_1 PR + a_2 PR^2$$
$$\eta_{vol}(PR) = b_0 - b_1 (PR^{1/\kappa} - 1)$$
$$\eta_{mech}(N) = c_0 + c_1 N + c_2 N^2$$

**체적효율 식은 물리에서 유도됩니다.** 압축실에는 완전히 배출되지 않는 클리어런스 체적이 남고, 이 고압 가스가 흡입 행정에서 재팽창하면서 새 냉매가 들어올 공간을 잠식합니다. 클리어런스 체적비를 $C$ 라 하면

$$\eta_{vol} = 1 - C(PR^{1/\kappa} - 1)$$

즉 $b_0 \approx 1$, $b_1 \approx C$ 입니다. 스크롤은 $C \approx 0.01\text{–}0.03$, 로터리는 $0.02\text{–}0.05$ 로 더 큽니다. 이것이 프리셋 차이의 실체입니다.

$\eta_{isen}$ 은 설계 압력비 부근에서 최대이고 양쪽으로 떨어지는 아래로 볼록한 형태이므로 $a_2 < 0$ 입니다.

> **가드레일**: 모든 효율은 $(0, 1]$ 로 클램프되고, 유효 압력비 구간(1.2 ~ 8.0)을 벗어나면 `EFFICIENCY_EXTRAPOLATED` 경고가 붙습니다. 상관식은 외삽하면 조용히 비물리적 값을 냅니다.

## 6.5 냉매별 특성 — 무엇이 왜 갈리는가

지원 냉매는 **R32 / R410A / R290** 세 가지입니다. 세 물질의 거동 차이는 두 개의 물성이 지배합니다.

| 물성 | R32 | R410A | R290 |
|---|---|---|---|
| 몰 질량 $M$ [g/mol] | 52.0 | 72.6 | 44.1 |
| 비열비 $\gamma$ | **1.29** | 1.19 | **1.13** |
| 비기체상수 $R = R_u/M$ [J/(kg·K)] | 159.8 | 114.5 | **188.6** |
| 임계 온도 $T_c$ [°C] | 78.1 | 71.3 | **96.7** |
| 임계 압력 $P_c$ [MPa] | 5.78 | 4.90 | 4.25 |
| 안전 등급 | A2L (미미한 가연성) | A1 (불연) | **A3 (가연성)** |

### ① 비열비 $\gamma$ → 토출 온도

등엔트로피 압축의 온도 상승비는

$$\frac{T_{2s}}{T_1} = PR^{(\gamma-1)/\gamma}$$

지수 $(\gamma-1)/\gamma$ 가 R32 는 0.225, R410A 는 0.160, R290 은 **0.115** 입니다. 같은 압력비에서도 온도 상승이 확연히 다릅니다.

### ② 비기체상수 $R$ → 단위 질량당 일

$$w_{isen} \propto R\,T_1\left[PR^{(\gamma-1)/\gamma}-1\right]$$

몰 질량이 작을수록 $R$ 이 크고 단위 질량당 일이 큽니다. R290 이 가장 큽니다.

### ③ 증기 밀도 → 체적 능력

$$\dot m = \rho_{suc} V_{disp} N \eta_{vol}$$

몰 질량이 작으면 같은 압력·온도에서 증기 밀도가 낮습니다. **R290 은 R32 의 절반 수준**입니다.

### 표준 조건 비교 (증발 7 °C / 응축 45 °C, 과열 5 K, 20 cc/rev, 3600 rpm, 상수 효율 0.70/0.95/0.95)

| 항목 | R32 | R410A | R290 |
|---|---|---|---|
| 압력비 $PR$ | 2.716 | 2.768 | **2.622** |
| 흡입 밀도 [kg/m³] | 25.3 | **35.4** | 12.2 |
| 등엔트로피 일 [kJ/kg] | 44.9 | 31.1 | **48.8** |
| 질량 유량 [kg/h] | 103.9 | **145.1** | 50.2 |
| **부하 토크 [N·m]** | **5.17** | 4.99 | **2.71** |
| **토출 온도 [°C]** | **114.6** | 83.9 | **59.8** |
| 샤프트 동력 [W] | 1950 | 1882 | 1022 |

> 위 값은 참조 백엔드(근사) 기준입니다. CoolProp 설치 시 절대값은 달라지지만 **순서 관계는 유지**됩니다. 이 순서는 지배 방정식에서 유도되므로 테스트로 고정되어 있습니다.

### 설계적 함의

**R32 — 뜨겁다**
토출 온도가 다른 두 냉매보다 30 ~ 55 K 높습니다. §6.6 의 열적 게이트를 실제로 트리거하는 유일한 냉매이며, 오일 선정과 모터 냉각 설계가 까다롭습니다. 대신 체적 능력이 좋아 압축기를 작게 만들 수 있습니다.

**R410A — 무겁다**
증기 밀도가 가장 높아 같은 행정체적에서 유량이 가장 큽니다. 단위 질량당 일이 작아 토크는 R32 와 비슷합니다. 임계 압력이 낮아(4.90 MPa) 고온 응축 시 임계점에 가장 먼저 접근합니다.

**R290 — 시원하지만 크다**
토출 온도가 60 °C 수준으로 열적 여유가 압도적입니다. 임계 온도도 96.7 °C 로 높아 고온 응축에서 유리합니다. **문제는 체적 능력**입니다 — 같은 냉방 능력을 내려면 **행정체적을 약 2배로 키우거나 회전수를 2배로 올려야** 합니다. 위 표에서 토크가 절반인 것은 능력이 절반이라는 뜻입니다.

> ⚠️ **R290 안전 고지**
> R290(프로판)은 **A3 등급 가연성 냉매**입니다. 본 시뮬레이터는 열역학·전자기 타당성만 판별하며, **충전량 제한(IEC 60335-2-40), 누설 감지, 환기, 방폭 설계, 시공 규정은 전혀 다루지 않습니다.** 해당 규격은 반드시 별도로 검토하십시오.

### R290 으로 동등 능력을 내려면

R32 설계를 R290 으로 옮길 때의 1차 근사입니다.

$$\frac{V_{disp,R290}}{V_{disp,R32}} \approx \frac{\rho_{suc,R32} \cdot w_{isen,R32}}{\rho_{suc,R290} \cdot w_{isen,R290}}$$

표의 값을 넣으면 $\dfrac{25.3 \times 44.9}{12.2 \times 48.8} \approx 1.91$ — 즉 **행정체적 약 1.9배**가 필요합니다.

시뮬레이터에서 확인하려면 `행정체적`을 20 → 38 cc/rev 로 바꾸고 토크와 동력이 R32 수준으로 오르는지 보십시오. 그 다음 그 토크가 모터의 전류·전압 한계 안에 들어오는지가 이 도구가 답하는 질문입니다.


## 6.6 열적·기계적 게이트

전자기적으로 가능해도 물리적으로 파괴적일 수 있습니다. 다음은 **독립적으로** 검사됩니다.

| 코드 | 조건 | 등급 | 물리적 이유 |
|---|---|---|---|
| `DISCHARGE_TEMP_HIGH` | $T_{dis} > 125$ °C | WARN | POE/PVE 냉동기유 탄화·산화 개시 |
| `DISCHARGE_TEMP_HIGH` | $T_{dis} > 135$ °C | FAIL | 유막 파단, 절연 열화 |
| `MAGNET_DEMAG_RISK` | $T_{magnet} > 120$ °C | FAIL | NdFeB 비가역 감자 |
| `LIQUID_SLUGGING` | $\Delta T_{sh} \le 0$ 또는 흡입 2상 | FAIL | 비압축성 액체가 실린더에 갇혀 순간 압력 폭증 |
| `LOW_SUPERHEAT` | $0 < \Delta T_{sh} < 3$ K | WARN | 센서 오차·과도 상태 여유 부족 |
| `NEGATIVE_SUBCOOL` | $\Delta T_{sc} < 0$ | FAIL | 팽창밸브 입구 플래시 가스 |
| `PR_OUT_OF_RANGE` | $PR > 8$ 또는 $< 1.2$ | WARN | 단단 압축 범위 이탈 |

### ⚠️ 자석 온도 오프셋의 부호 — 반드시 확인하십시오

자석 온도는 다음으로 추정합니다.

$$T_{magnet} = T_{dis} + \Delta T_{offset}, \qquad \Delta T_{offset} = -10\ \mathrm{K}\ (\text{기본값})$$

**오프셋이 음수인 이유**: 자석은 압축 직후 최고온 토출 가스보다 낮은 온도에서 평형을 이룹니다(셸 내부 혼합·방열).

이 값은 **모터 냉각 구조에 따라 달라지며, 현재 값은 공학적 가정**입니다. `plan.md` §10.5 C7 에 확인 요청 항목으로 등록되어 있습니다.

- 흡입 가스 냉각형 → 더 큰 음수 (예: −30 K)
- 자석이 토출부에 직접 노출되는 특수 구조 → 양수

### 게이트 순서 정합성

세 임계는 토출 온도 축에서 다음 순서를 지켜야 합니다.

$$\underbrace{125\ °\mathrm{C}}_{\text{오일 경고}} < \underbrace{130\ °\mathrm{C}}_{\text{감자 발동}} < \underbrace{135\ °\mathrm{C}}_{\text{오일 한계}}$$

여기서 감자 발동 온도는 $T_{demag} - \Delta T_{offset} = 120 - (-10) = 130$ °C 입니다.

> **왜 이것이 중요한가**: 개발 중 실제로 발견된 결함입니다. 초기 설정($\Delta T_{offset} = +10$ K)에서는 감자 게이트가 $T_{dis} > 110$ °C 에 발동해 **125 °C 오일 경고선이 영원히 도달 불가**가 되었습니다. 한 게이트가 다른 게이트를 완전히 가린 것입니다. 이 순서를 깨는 설정을 넣으면 GUI 상태 표시줄에 경고가 나옵니다.

---

# 7. 공학적 배경 II. PMSM 전자기

## 7.1 왜 dq 좌표계인가

3상 모터의 전류는 시간에 따라 정현파로 변합니다. 이대로는 제어가 어렵습니다.

**핵심 아이디어**: 로터와 **함께 회전하는** 좌표계에서 보면, 정상상태의 정현파 전류가 **직류(상수)** 로 보입니다. 마치 회전목마 위에서 다른 목마를 보면 정지해 보이는 것과 같습니다.

이 회전 좌표계의 두 축을 이렇게 정의합니다.

- **d축 (direct)**: 자석의 자속 방향과 나란한 축
- **q축 (quadrature)**: d축에서 전기각 90° 앞선 축

물리적 의미가 명확히 갈립니다.

- $i_q$ → 자속과 직교 → **토크를 만든다**
- $i_d$ → 자속과 나란함 → **자속을 강화하거나 약화시킨다**

## 7.2 정상상태 전압 방정식

회전 좌표계에서 정상상태($di/dt = 0$)의 전압은

$$v_d = R_s i_d - \omega_e L_q i_q$$
$$v_q = R_s i_q + \omega_e (L_d i_d + \lambda_{pm})$$

각 항의 의미:

| 항 | 이름 | 설명 |
|---|---|---|
| $R_s i_d$, $R_s i_q$ | 저항 강하 | 권선 저항. 저속에서 상대 비중이 큼 |
| $-\omega_e L_q i_q$ | 교차 결합 | q축 전류가 만드는 자속이 d축 전압에 나타남 |
| $\omega_e L_d i_d$ | 전기자 반작용 | $i_d < 0$ 이면 이 항이 음수 → 자속 약화 |
| $\omega_e \lambda_{pm}$ | **역기전력** | 자석이 만드는 전압. 속도에 비례 |

**$\omega_e \lambda_{pm}$ 이 이 도구의 주인공입니다.** 속도가 오르면 이 항만 계속 커지고, 결국 인버터가 낼 수 있는 전압을 넘어섭니다.

## 7.3 토크 방정식

$$T_{em} = \frac{3}{2}p\left[\underbrace{\lambda_{pm} i_q}_{\text{마그넷 토크}} + \underbrace{(L_d - L_q) i_d i_q}_{\text{릴럭턴스 토크}}\right]$$

**제1항(마그넷 토크)**: 자석 자속과 $i_q$ 의 상호작용. 직관적입니다.

**제2항(릴럭턴스 토크)**: 로터의 자기적 비대칭(돌극성)에서 나옵니다. 자속은 자기저항이 낮은 경로를 선호하고, 로터는 그 방향으로 정렬되려는 토크를 받습니다.

IPMSM 은 $L_q > L_d$ 이므로 $(L_d - L_q) < 0$ 입니다. 따라서:

$$i_d < 0 \implies (L_d - L_q) i_d i_q > 0 \implies \text{토크 증가}$$

**이것이 결정적입니다.** 음의 $i_d$ 는 (a) 릴럭턴스 토크를 만들고 (b) 자속을 약화시켜 고속 운전을 돕습니다. **두 목적이 같은 방향을 가리킵니다.**

SPMSM($L_d = L_q$)은 제2항이 사라져 MTPA 가 $i_d = 0$ 입니다.

## 7.4 구속 조건 ① — 전류 한계 원

$$i_d^2 + i_q^2 \le i_{max}^2$$

dq 평면에서 **원점 중심, 반지름 $i_{max}$ 인 원**입니다. 소자 정격과 권선 열 정격 중 작은 값으로 정합니다. 속도와 무관하게 고정입니다.

## 7.5 구속 조건 ② — 전압 한계 타원

$R_s$ 를 무시하면 (고속에서 타당) 전압 조건 $v_d^2 + v_q^2 \le v_{max}^2$ 는

$$(\omega_e L_q i_q)^2 + (\omega_e L_d i_d + \omega_e\lambda_{pm})^2 \le v_{max}^2$$

양변을 $\omega_e^2$ 로 나누면

$$\boxed{\ \left(L_d i_d + \lambda_{pm}\right)^2 + \left(L_q i_q\right)^2 \le \left(\frac{v_{max}}{\omega_e}\right)^2\ }$$

이는 다음 **타원**입니다.

| 요소 | 값 | 속도 의존성 |
|---|---|---|
| 중심 | $\left(-\dfrac{\lambda_{pm}}{L_d},\ 0\right)$ | **없음** (고정) |
| $i_d$ 방향 반축 | $\dfrac{v_{max}}{\omega_e L_d}$ | $\propto 1/\omega_e$ |
| $i_q$ 방향 반축 | $\dfrac{v_{max}}{\omega_e L_q}$ | $\propto 1/\omega_e$ |

**속도가 오르면 타원이 중심점으로 수축합니다.** 이것이 "동작 불가(전압 제한)"의 물리적 실체입니다.

### 수치 예 (기본 파라미터)

| 회전수 | $\omega_e$ [rad/s] | 반축 $i_d$ [A] | 반축 $i_q$ [A] | 중심 $i_d$ [A] |
|---|---|---|---|---|
| 3600 rpm | 1131 | 50.1 | 25.1 | −26.7 |
| 36000 rpm | 11310 | 5.0 | 2.5 | −26.7 |

36000 rpm 에서 타원은 $i_d \in [-31.7,\ -21.7]$ 구간에만 존재합니다. 전류 원($|i| \le 20$)과 **완전히 분리**되므로 어떤 토크도 낼 수 없습니다 — 최대 가용 토크가 정확히 0 이 됩니다.

## 7.6 MTPA — 최소 전류로 최대 토크

같은 토크를 내는 $(i_d, i_q)$ 조합은 무수히 많습니다. 그중 **전류 크기가 최소**인 점을 고르는 것이 MTPA(Maximum Torque Per Ampere)이고, 이는 동손 최소화를 뜻합니다.

### 유도

전류 크기를 $I$ 로 고정하고 각도로 파라미터화합니다.

$$i_d = -I\sin\beta, \qquad i_q = I\cos\beta$$

돌극성을 $\Delta = L_q - L_d > 0$ 로 두면 토크는

$$T = \frac{3}{2}p\left[\lambda_{pm} I\cos\beta + \Delta I^2 \sin\beta\cos\beta\right] = \frac{3}{2}p\left[\lambda_{pm} I\cos\beta + \frac{\Delta I^2}{2}\sin 2\beta\right]$$

$\beta$ 로 미분해 0 으로 두면

$$-\lambda_{pm} I\sin\beta + \Delta I^2\cos 2\beta = 0$$

$\cos 2\beta = 1 - 2\sin^2\beta$ 를 대입하면 $\sin\beta$ 에 대한 2차식이 되고, 이를 $i_d$ 로 정리하면

$$\Delta u^2 - \lambda_{pm} u - \Delta i_q^2 = 0, \qquad u \equiv i_d$$

근의 공식에서 음의 근(물리적으로 $i_d < 0$)을 취하면

$$\boxed{\ i_d = A - \sqrt{A^2 + i_q^2}\ }, \qquad A = \frac{\lambda_{pm}}{2(L_q - L_d)}$$

이것이 코드의 `mtpa_id()` 입니다. $L_q = L_d$ 면 $A \to \infty$ 로 발산하므로 SPMSM 은 별도 분기($i_d = 0$)로 처리합니다.

## 7.7 약계자 — 전압의 벽을 넘기

속도가 올라 MTPA 점이 전압 타원 밖으로 밀려나면 어떻게 할까요?

$v_q$ 식을 다시 봅니다.

$$v_q = R_s i_q + \omega_e (L_d i_d + \lambda_{pm})$$

$i_d$ 를 **음수**로 만들면 $L_d i_d$ 가 $\lambda_{pm}$ 을 상쇄해 필요 전압이 내려갑니다. 자석 자속을 전기적으로 "약화"시키는 것이므로 **약계자(flux weakening)** 라 부릅니다.

대가가 있습니다. 같은 토크를 유지하려면 $i_q$ 를 조정해야 하고, 결과적으로 **전류 크기가 커집니다**(MTPA 를 벗어났으므로). 그래서 UI 에서 약계자 진입 시 $|i|$ 가 증가하는 것이 보입니다.

### 특성 전류 — 이 모터의 운명을 결정하는 값

$$i_{ch} = \frac{\lambda_{pm}}{L_d}$$

전압 타원 중심의 $i_d$ 좌표 크기입니다.

| 조건 | 결과 |
|---|---|
| $i_{ch} < i_{max}$ | 타원 중심이 전류 원 **안**. 이론상 **무한 속도** 구동 가능 |
| $i_{ch} > i_{max}$ | 타원 중심이 전류 원 **밖**. **유한한 최대 속도** 존재 |

기본 파라미터는 $i_{ch} = 26.7$ A > $i_{max} = 20$ A 이므로 후자입니다. 고속에서 반드시 한계에 부딪히며, 속도 스윕 탭의 빨강 파선이 그 지점입니다.

> **설계 인사이트**: 고속 운전이 필요한데 최대 속도가 부족하다면 선택지는 세 가지입니다. ① $V_{dc}$ 를 올린다 ② $\lambda_{pm}$ 을 줄인다(자석 감소, 저속 토크 희생) ③ $L_d$ 를 늘린다(설계 변경). $i_{ch}$ 를 $i_{max}$ 아래로 낮추는 것이 ②③ 의 목표입니다.

## 7.8 판정 알고리즘

프로그램은 문제를 **1차원**으로 축소해 풉니다.

토크 등식이 미지수 2개에 등식 1개를 주므로, 해집합은 곡선입니다. $i_q$ 로 파라미터화하면

$$i_d(i_q) = \frac{\dfrac{T}{\tfrac32 p\, i_q} - \lambda_{pm}}{L_d - L_q}$$

이제 이 곡선 위에서만 탐색하면 됩니다.

```
S1. MTPA 궤적에서 낼 수 있는 최대 토크 계산 (전류 한계까지)
S2. T_load 가 그보다 크면 → CURRENT_LIMIT
S3. MTPA 점이 전압 타원 안이면 → 완료 (mode = MTPA)
S4. 아니면 상수 토크 곡선 위에서 전압을 만족하는 최소 전류점 탐색
       찾으면 → 완료 (mode = FLUX_WEAKENING)
       못 찾으면 → 원인 분해:
         곡선이 전압 타원과 아예 안 만남      → VOLTAGE_LIMIT
         각각은 만족하나 교집합이 없음        → BOTH_LIMIT
```

원인 분해는 다음 집합 정의를 그대로 구현한 것이라 **누락도 중복도 없습니다**.

$$\mathcal{S} = \left\{(i_d,i_q) \;\middle|\; T_{em} = T_{load} \;\wedge\; |i| \le i_{max} \;\wedge\; |v| \le v_{max}\right\}$$

$\mathcal{S} \ne \emptyset$ 이면 가능, 그중 $|i|$ 최소점이 동작점입니다.

## 7.9 판정 기준과 화면의 차이

| 용도 | 사용 식 |
|---|---|
| **판정** | $R_s$ **포함** 정확식 |
| 시각화(초록 실선) | $R_s$ 무시 근사 타원 |
| 시각화(연초록 파선) | $R_s$ 포함 정확 경계 |

저속·고토크에서 $R_s i$ 강하가 무시할 수 없기 때문입니다. 두 선의 간격이 저속에서 벌어지는 것을 UI 에서 직접 확인할 수 있습니다.

---

# 8. 공학적 배경 III. 두 도메인의 결합

## 8.1 인터페이스는 단 하나

```
CycleSpec + CompressorSpec ──[열역학]──▶ (T_load, ω_m) ──[전자기]──▶ OperatingPoint
```

두 도메인은 **2-튜플 하나로만** 대화합니다. 전자기 계산은 냉매가 무엇이었는지, 압력이 얼마였는지 **알지 못하고 알 필요도 없습니다.**

이 좁은 인터페이스가 주는 이점:

- **단방향** → 반복 수렴 루프 불필요, 순수 함수 파이프라인
- **독립 검증** → 각 도메인을 따로 테스트 가능
- **낮은 결합도** → 한쪽 모델을 바꿔도 다른 쪽에 파급 없음

## 8.2 결정적 통찰 — 부하 토크는 속도와 (거의) 무관하다

`compute_load_point()` 의 계산을 대수적으로 정리하면:

$$T_{load} = \frac{P_{shaft}}{\omega_m} = \frac{\dot m \cdot w_{isen}}{\eta_{isen}\eta_{mech} \cdot 2\pi N}$$

여기에 $\dot m = \rho_{suc} V_{disp} N \eta_{vol}$ 을 대입하면

$$T_{load} = \frac{\rho_{suc} V_{disp} \cancel{N} \eta_{vol} \cdot w_{isen}}{\eta_{isen}\eta_{mech} \cdot 2\pi \cancel{N}} = \boxed{\ \frac{\rho_{suc} V_{disp}\,\eta_{vol}\, w_{isen}}{2\pi\, \eta_{isen}\eta_{mech}}\ }$$

**$N$ 이 소거됩니다.**

용적형 압축기의 부하 토크는 회전수와 무관하고 **압력비와 흡입 상태에만 의존**합니다. 물리적으로 당연합니다 — 한 바퀴당 밀어내는 부피가 고정이므로, 한 바퀴당 하는 일도 고정이고, 토크는 곧 "회전각당 일"이기 때문입니다.

실제로는 $\eta_{vol}$, $\eta_{mech}$ 의 속도 의존성 때문에 약한 기울기가 남습니다. 그래서 속도 스윕 탭의 주황 선이 **거의** 수평입니다.

### 이 성질이 만드는 비대칭

| 속도가 오르면 | 변화 |
|---|---|
| 부하 토크 (주황) | 거의 불변 |
| 전압 타원 | $1/\omega_e$ 로 축소 |
| 최대 가용 토크 (초록) | 감소 |

**두 선은 반드시 만납니다.** 그 교점이 최대 가동 회전수이고, 이 도구가 답하는 핵심 질문입니다.

## 8.3 전체 판정 흐름

```
Step 0  사전 위생 검사    압력 역전? 초임계?
   ↓
Step 1  열역학            상태점 → w_isen → 효율 → T_load, ω_e
   ↓
Step 2  전자기            MTPA / 약계자 동작점 탐색
   ↓
Step 3  구속 조건         전류 원 / 전압 타원 판정, 여유율 계산
   ↓
Step 4  열적·기계적 게이트  (전자기 결과와 **독립** 실행)
   ↓
Verdict  FEASIBLE / FEASIBLE_WITH_WARNING / INFEASIBLE
```

**Step 4 가 독립인 이유**: 전자기적으로 불가한 경우에도 열적 위반을 함께 보여줘야 설계 피드백이 됩니다. "전압도 모자라고 토출 온도도 넘는다"는 정보가 "전압이 모자란다"보다 유용합니다.

## 8.4 왜 이 도구를 신뢰할 수 있는가

정확성 검증과 회귀 감지를 **분리**했습니다. 이 구분이 무너지면 "틀린 값이 고정되어 영원히 통과하는" 함정에 빠집니다.

| 목적 | 방법 | 허용오차 |
|---|---|---|
| **정확성** | 독립 경로 교차 검증 — 테스트가 리터럴 물성 상수로 직접 계산 | 10 % / 5 % |
| **회귀 감지** | `tests/golden/*.json` 고정 값 | 1e-6 |
| **물리 일관성** | 단조성·항등성 등 불변식 | 해석적 |

**독립 경로란**: CoolProp 결과를 CoolProp 로 검증하면 동어반복입니다. 그래서 테스트가 문헌 물성 상수($M$, $\gamma$, $Z$)를 직접 갖고 실기체 보정 폴리트로픽 식으로 별도 계산해 대조합니다.

$$w_{ref} = Z \cdot \frac{\gamma}{\gamma-1} R T_1\left[PR^{(\gamma-1)/\gamma} - 1\right]$$

### 손계산 대조 결과

표준 R32 사이클(7/45 °C, 20 cc/rev, 3600 rpm, 상수 효율 0.70/0.95/0.95):

| 항목 | 손계산 | 구현 |
|---|---|---|
| $T_{load}$ | 5.17 N·m | 5.172 N·m |
| $(i_d, i_q)$ @3600 rpm | ≈ (−5, 12.5) A | (−4.74, 12.20) A |
| $\|v\|$ @3600 rpm | ≈ 118 V | 117.2 V |
| $T_{max}^{avail}$ @36000 rpm | 0 (영역 분리) | 0.000 |

---

# 9. 문제 해결 및 한계

## 9.1 판정 코드별 대응

| 코드 | 의미 | 우선 확인할 것 |
|---|---|---|
| `PRESSURE_INVERSION` | 토출 압력 ≤ 흡입 압력 | 응축/증발 조건이 뒤바뀌었거나, 압력손실 입력이 과대. 물성 계산 전 즉시 중단됩니다 |
| `SUPERCRITICAL` | 응축 압력 ≥ 임계 압력 | 응축 온도 과다. R32 는 $P_c = 5.78$ MPa($T_c = 78.1$ °C), R410A 는 4.90 MPa($T_c \approx 71.3$ °C). 초임계는 본 모델 범위 밖입니다 |
| `CURRENT_LIMIT` | 전류 한계 내에서 토크 부족 | $i_{max}$ 가 peak 인가(§4.2)? $\lambda_{pm}$·$p$ 가 맞는가? 행정체적이 과대한가? |
| `VOLTAGE_LIMIT` | 전압 타원 밖 | $V_{dc}$ 가 실제 값인가? 속도가 과한가? $\lambda_{pm}$ 이 과대한가? |
| `BOTH_LIMIT` | 각각은 되나 교집합 없음 | 전형적 고속 문제. $i_{ch}$ 를 확인(§7.7) |
| `BACK_EMF_EXCEEDS_VMAX` | 역기전력 > $v_{max}$ (WARN) | 약계자로 극복 가능하나 원인 규명 단서 |
| `DISCHARGE_TEMP_HIGH` | 토출 온도 초과 | 응축 온도 과다? 과열도 과다? $\eta_{isen}$ 과소? |
| `MAGNET_DEMAG_RISK` | 자석 감자 위험 | **먼저 `dT_magnet_offset` 부호를 확인**(§6.6) |
| `LIQUID_SLUGGING` | 액 압축 | 과열도가 0 이하 |
| `PR_OUT_OF_RANGE` | 압력비 이탈 | 상관식 외삽 구간 — 결과 신뢰도 하락 |
| `EFFICIENCY_EXTRAPOLATED` | 효율 외삽 | 동상 |
| `REFERENCE_BACKEND_IN_USE` | CoolProp 미사용 | **설계 판단 금지** (§1.3) |
| `SOLVER_NOT_CONVERGED` | 수치 해 실패 | 파라미터가 극단적. 값 범위 재확인 후 재현되면 보고 |

## 9.2 자주 겪는 증상

**증상: 토크가 예상의 약 1.4 배 또는 0.7 배**
→ RMS/peak 혼동입니다. §4.2 확인.

**증상: 토크가 정확히 2 배 또는 0.5 배**
→ 극수/극쌍수 혼동입니다. §3.4 확인.

**증상: 저속인데 전압 제한에 걸림 / 토크가 예상의 1.73 배**
→ $K_e$ 기준(선간/상) 혼동입니다. §3.3·§5.4 확인. `--show-motor` 의 `1000rpm 선간 역기전력` 을 실측값과 대조하십시오.

**증상: MTPA 가 $i_d > 0$ 을 선택**
→ $L_q < L_d$ 입니다. 인덕턴스가 뒤바뀌었을 가능성. §3.2 확인.

**증상: 표준 조건인데 감자 위험이 뜸**
→ `dT_magnet_offset` 부호를 확인하십시오. §6.6.

**증상: 결과가 문서의 5.172 N·m 와 다름**
→ 기본 프리셋은 스크롤(가변 효율)이라 5.235 N·m 가 나옵니다. 5.172 는 상수 효율(0.70/0.95/0.95) 기준값입니다.

## 9.3 현재 지원하지 않는 것

솔직하게 명시합니다. 아래는 **아직 구현되지 않았습니다.**

| 항목 | 상태 |
|---|---|
| **3D 플롯** | 미지원. 현재 시각화는 dq 평면과 속도 스윕 **2D 뷰 2종**입니다 |
| **Hz↔rpm 자동 변환** | 미지원. §4.4 절차로 손으로 환산해 입력하십시오 |
| ~~선간↔상 자동 변환~~ | **v0.2 에서 구현됨** — 역기전력은 기준 콤보(§5.4), 인덕턴스·저항은 측정 단계 안내(§3.2·§3.5) |
| ~~쇄교자속 직접 입력~~ | **v0.2 에서 Ke 입력으로 전환** — λpm 은 파생값으로 표시 |
| 과도 응답 (기동 토크, 관성) | v1 범위 밖. 정상상태 전용 |
| 모터 손실의 사이클 되먹임 | 단방향 파이프라인 유지 |
| 크랭크 각도별 순시 토크 리플 | 평균 토크만 |
| FEA 기반 정밀 감자 판별 | 외부 데이터 필요 |
| 인젝션 / 2단 압축 | 단단 압축 한정 |
| 온도 의존 파라미터 ($R_s(T)$, $\lambda_{pm}(T)$) | 상온 상수 가정 |
| 과변조 / 6-step | 선형 변조 영역만 |
| ~~R290~~ | **v0.3 에서 추가됨** (§6.5) |
| R454B, R1234yf 등 추가 냉매 | CoolProp 지원 물질이므로 확장은 용이 |

## 9.4 이 도구의 위치

**본 도구는 스크리닝(screening) 도구이며 인증 도구가 아닙니다.**

설계 초기에 "이 조합이 물리적으로 말이 되는가"를 빠르게 걸러내는 용도입니다. 최종 검증은 실측과 FEA 로 하십시오. 특히 자석 감자 판별은 자기회로 FEA 없이는 정밀도를 보장할 수 없습니다.

---

## 부록 A. 기호 일람

### 열역학

| 기호 | 의미 | SI | UI |
|---|---|---|---|
| $P_{evap}, P_{cond}$ | 증발/응축 압력 | Pa | kPa |
| $T_{suc}, T_{dis}$ | 흡입/토출 온도 | K | °C |
| $\Delta T_{sh}, \Delta T_{sc}$ | 과열도 / 과냉도 | K | K |
| $h, s$ | 비엔탈피 / 비엔트로피 | J/kg, J/(kg·K) | kJ/kg |
| $\rho_{suc}$ | 흡입 밀도 | kg/m³ | kg/m³ |
| $\dot m$ | 질량 유량 | kg/s | kg/h |
| $V_{disp}$ | 행정체적 | m³/rev | cc/rev |
| $PR$ | 압력비 | – | – |
| $\gamma$ | 비열비 | – | – |

### 전자기

| 기호 | 의미 | SI | UI |
|---|---|---|---|
| $L_d, L_q$ | d/q축 인덕턴스 (**상**) | H | mH |
| $\lambda_{pm}$ | 쇄교자속 (상 peak) | Wb | **파생값** (표시 전용) |
| $K_e$ | 역기전력 상수 | – | **Vrms/krpm** (선간 기본) |
| $p$ | **극쌍수** | – | – |
| $R_s$ | 상저항 (상당) | Ω | Ω |
| $i_d, i_q$ | dq 전류 (peak) | A | A |
| $v_d, v_q$ | dq 전압 (peak) | V | V |
| $i_{max}, v_{max}$ | 전류/전압 한계 (peak) | A, V | A, V |
| $\omega_m, \omega_e$ | 기계/전기 각속도 | rad/s | rpm |
| $i_{ch}$ | 특성 전류 $\lambda_{pm}/L_d$ | A | A |
| $T_{load}, T_{em}$ | 부하/전자기 토크 | N·m | N·m |

## 부록 B. 핵심 수식 요약

$$v_{max} = k_{margin}\frac{V_{dc}}{\sqrt3}\ \text{(SVPWM)}$$

$$T_{em} = \frac{3}{2}p\left[\lambda_{pm} i_q + (L_d-L_q)i_d i_q\right]$$

$$i_d^{MTPA} = A - \sqrt{A^2+i_q^2},\quad A = \frac{\lambda_{pm}}{2(L_q-L_d)}$$

$$\left(L_d i_d + \lambda_{pm}\right)^2 + \left(L_q i_q\right)^2 \le \left(\frac{v_{max}}{\omega_e}\right)^2$$

$$T_{load} = \frac{\rho_{suc} V_{disp}\,\eta_{vol}\, w_{isen}}{2\pi\,\eta_{isen}\eta_{mech}}$$

$$\lambda_{pm} = \frac{60\sqrt2}{2\pi\cdot 1000\,\sqrt3\,p}K_e \ \text{(선간)},\qquad
\lambda_{pm} = \frac{60\sqrt2}{2\pi\cdot 1000\,p}K_e \ \text{(상)}$$

$$E_{LL,rms}(N) = \frac{\sqrt3}{\sqrt2}\,\omega_e \lambda_{pm},\qquad \omega_e = \frac{2\pi p N}{60}$$

## 부록 C. 참고 문헌

- S. Morimoto et al., "Expansion of Operating Limits for PM Motor by Current Vector Control Considering Inverter Capacity", *IEEE Trans. Ind. Appl.*, 1990 — 전류/전압 제한 원·타원 및 MTPA 원전
- W. Leonhard, *Control of Electrical Drives* — dq 모델 및 약계자 이론
- ASHRAE Handbook — Refrigeration, Compressor 장 — 효율 상관식
- I. Bell et al., "Pure and Pseudo-pure Fluid Thermophysical Property Evaluation and the Open-Source Thermophysical Property Library CoolProp", *Ind. Eng. Chem. Res.*, 2014
- AHRI Standard 540 — 압축기 성능 10계수 다항식 모델

---

**관련 문서**: [`research.md`](research.md) 개념 정의 · [`plan.md`](plan.md) 구현 계획 및 변경 이력 · [`README.md`](README.md) 요약
